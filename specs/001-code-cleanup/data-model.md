# Data Model: Очистка кода конструктора

**Date**: 2026-03-28
**Feature**: 001-code-cleanup

## Изменения в данных

Эта задача не вводит новые сущности. Единственное изменение в модели данных — исправление дублирующихся ID в объекте `products`.

### Исправление ID товаров

#### Наволочки (pillows.prints) — дублирующийся `"pillows14"`

| Товар | Текущий ID | Новый ID |
|-------|-----------|----------|
| БОН ПАРИ | pillows14 | pillows14 (оставить — первый с этим ID) |
| ЛЕЙКА | pillows14 | pillows30 |
| ЛУГ | pillows14 | pillows31 |
| РАСКРАСКА | pillows14 | pillows32 |
| ТИХИЙ ЧАС | pillows14 | pillows33 |
| ТОЧКА ЗАПЯТАЯ | pillows14 | pillows34 |

#### Простыни (mattresses.solids) — дублирующийся `"mattresses9"`

| Товар | Текущий ID | Новый ID |
|-------|-----------|----------|
| ТОПЛЕНОЕ МОЛОКО | mattresses9 | mattresses9 (оставить) |
| НЕБО | mattresses9 | mattresses10 |

### Удаляемые переменные/состояние

| Элемент | Тип | Причина удаления |
|---------|-----|------------------|
| `currentCategory` | `let` переменная | Не читается |
| `notification` | `const` переменная | Заменяется на inline-доступ через `document.getElementById` |
| Комментированный `sizes` | комментарий | Мёртвый код |
| `localStorage['beddingSelections']` | хранилище | Пишется, но не читается |

### Добавляемые HTML-элементы

| Элемент | Расположение | Назначение |
|---------|-------------|------------|
| `<div id="notification" class="notification"></div>` | Внутри `.customizer-container` | Отображение уведомлений пользователю |
